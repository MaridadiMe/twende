import 'package:flutter/material.dart';

class DriverTrips extends StatelessWidget {
  const DriverTrips({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text('List of trips for the driver will be shown here.'),
      ),
    );
  }
}
